feedparser>=6.0.11
Jinja2>=3.1.4
PyYAML>=6.0.2
requests>=2.32.3
python-dateutil>=2.9.0

# Analysis uses Google Gemini or Groq (both free tiers) over plain HTTP via
# `requests` — no extra dependency needed.
#
# Only required if you choose the Anthropic/Claude provider:
#   pip install "anthropic>=0.49.0"
